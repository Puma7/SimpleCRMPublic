# Freigabeliste Teil 3: Codex-Befunde mit Nebenwirkung

**Stand:** 2026-09-25 · Grundlage: Triage aller 110 Einträge aus PR #192 (Anhang A 82, B 17, C 11) gegen das Befundregister, geprüft auf HEAD nach dem Merge (`37c5d02`).

## Entscheidung (2026-09-25)

Pascal hat **alle Empfehlungen G1–G12** freigegeben. Umsetzung und Commits stehen im Befundregister und im Abschlussbericht.

---

Codex' Einträge waren ungeprüfte Hypothesen. Jeder Eintrag wurde auf dem aktuellen Stand nachvollzogen. Duplikate, die unser Register schon behoben hat, und bestätigte Befunde ohne Nebenwirkung sind bereits behoben oder in Arbeit (siehe `abschlussbericht.md`, Abschnitt Codex). Hier stehen nur die Punkte, deren Fix legitimen Nutzern Rechte nimmt, einen API-Vertrag ändert, die Betriebsverdrahtung ändert oder mehrere vertretbare Semantiken hat. Die Empfehlung steht jeweils zuerst.

## A. Rechte und Autorisierung

| # | Befund | Problem | Empfehlung | Alternative |
|---|---|---|---|---|
| G1 | C-A1, C-A47, C-B1, C-A37 (mittel), revidiert F-A7-01 | Auf dem Desktop darf jede Rolle Workflows anlegen, ändern, importieren, Versionen wiederherstellen und den Backfill starten. Ein Code-Knoten (JavaScript/Python) läuft per Cron oder Inbound mit den Rechten des Electron-Prozesses. Damit hebelt ein Agent oder Viewer E15–E17 komplett aus. `ExecuteWorkflowNow` verlangt schon Owner/Admin, der Cron-Pfad nicht. Die frühere Einstufung „By-Design“ hält seit E15–E17 nicht mehr. | Workflow-Bearbeitung auf dem Desktop nur für Owner/Admin (create, update, delete, import, import-from-file, restore-version, backfill, Automation-Einstellungen). Der Editor ist für andere Rollen schreibgeschützt. | Server-Parität: Agenten dürfen nur deaktivierte oder seiteneffektfreie Graphen ohne Code und Cron speichern. |
| G2 | C-A20 (mittel, Server) | Wer `workflows.edit` hat, kann einen aktiven Workflow mit Seiteneffekt oder Kettenstopp (z. B. eine KI-Ausgangsprüfung) deaktivieren, durch einen harmlosen Graphen ersetzen oder per Restore zurücksetzen. Das Gate prüft nur den neuen Zustand. Ein Renderer-Test schreibt das heutige Verhalten fest. | Ist der gespeicherte Workflow aktiv und hat Seiteneffekt- oder Kettenstopp-Knoten, verlangen Änderungen an Aktivierung, Graph, Trigger, Konto und Zeitplan `workflows.manage`, symmetrisch zum Aktivieren. Metadaten und deaktivierte Entwürfe bleiben mit `edit` änderbar. | So lassen, aber jede solche Änderung auditieren. |
| G3 | C-A72 Teil 2 (niedrig), berührt A2b-05 | Ein Admin kann sich selbst zum Owner machen und Owner-Konten ändern. Damit erreicht er auf dem Desktop den Owner-only-Restore (E17) und auf dem Server den Owner-only-Hard-Reset. Der Schutz des letzten Owners ist schon behoben (`ff4dd01`). | In beiden Editionen vergeben oder entziehen nur Owner die Owner-Rolle und ändern Owner-Konten (Passwort, Aktiv, Rolle). Admins verwalten alle anderen. Ein Server-Test, der das heutige Verhalten festschreibt, wird angepasst. | Nur auf dem Desktop; oder so lassen und dokumentieren („Admin entspricht faktisch Owner“). |
| G4 | C-A36 (niedrig, Server) | `/ai/transform-text` lädt einen Kunden per `customerId` ohne `crm.read` und gibt Name und E-Mail an den KI-Anbieter. Der Composer sendet `customerId` automatisch mit. | Ohne `crm.read` wird `customerId` stillschweigend ignoriert (Platzhalter bleiben leer). Mail-Nutzer ohne CRM-Recht können weiter umformulieren. | 403, wenn `customerId` ohne `crm.read` kommt. |
| G5 | C-A79 (niedrig, Desktop, latent) | Beim Antworten wird eine Eltern-Mail aus einem fremden Konto für Header und Ticket genutzt und als erledigt markiert. Dafür braucht es eine `rw`-Freigabe, die die App heute nicht vergibt. | Den Elternbezug nur nutzen, wenn der Nutzer das Elternkonto lesen darf, sonst verwerfen. „Erledigt“ nur mit `rw`. | Antwort nur aus demselben Konto erlauben. |
| G6 | C-A2 Rest (niedrig, Desktop, latent) | Geplante Sendungen prüfen beim Versand nicht, ob der Planende noch Zugriff hat. | Akteur beim Planen speichern (ohne Migration, per `sync_info`). Beim Versand `rw` prüfen, bei Entzug auf HOLD setzen mit Hinweis. Ältere Planungen ohne gespeicherten Akteur laufen wie bisher. | So lassen (die App vergibt heute keine Konto-Freigaben). |

## B. Mail, Workflows, Betrieb

| # | Befund | Problem | Empfehlung | Alternative |
|---|---|---|---|---|
| G7 | C-A7, C-B8, C-C3 (hoch, beide) | Unser Budget für entpackte Bytes (F-A5-04) greift. Eine 48-KB-DOCX im Budget treibt mammoth aber auf 6,2 GB RSS und 18–34 s Blockade, bei 2 GB Heap endet das im OOM. „Vor dem Parse markieren“ gegen die Absturzschleife ist ohne Freigabe in Arbeit. | DOCX-Extraktion in einem `worker_threads`-Worker mit Heap-Limit (512 MB) und Abbruch beim Timeout, in beiden Editionen. Braucht ein zusätzliches Build-Artefakt für Docker und Electron. | Ohne Worker: Obergrenze von 500.000 XML-Tags. Größere DOCX kommen nicht in die Volltextsuche. |
| G8 | C-A75 (niedrig, beide), berührt E6 | `selectTrustedAuthenticationResults` nimmt das erste vertrauenswürdige Feld irgendwo im Header. Bei Gmail gewinnt ein gefälschtes tieferes Feld. | Nur das oberste `Authentication-Results`-Feld auswerten (RFC 8601 §5). Ist dessen authserv-id nicht vertrauenswürdig, gilt „unbekannt“. | Fallback nur bei ausdrücklich konfigurierter authserv-id (der Desktop bräuchte ein UI-Feld). |
| G9 | C-A61 + C-A55 Teil 2 (mittel, Infra) | `restore.sh` und `restore-drill.sh` verbinden als `simplecrm_admin` (Superuser). SQL aus einem manipulierten Dump kommt per `RESET ROLE` zurück zum Superuser. Beim Drill trifft das den Produktionscluster. | `pg_restore` und die Verifikation laufen als eingeschränkte Rolle (NOSUPERUSER, Eigentümer der Ziel-DB). Admin nur für DROP/CREATE DATABASE. Abgesichert über den CI-Smoke mit Restore-Drill. | Dokumentieren, dass nur vertrauenswürdige Backups eingespielt werden dürfen. |
| G10 | C-A66 (mittel, Server) | Der Ad-hoc-SMTP-Test mit eingegebenem Passwort ignoriert den TLS-Schalter; Passwort und `AUTH PLAIN` gehen im Klartext (per Probe gezeigt). | Optionales Feld `tls` im Request; bei `tls=true` und nicht Port 465 wird STARTTLS erzwungen. Fehlt das Feld (alte Clients), bleibt das Verhalten gleich. | – |
| G11 | C-A18, C-A76, C-C11 (mittel), nur Dry-Run-Frage | Der Read-only-SQL-Validator wird ohne Freigabe gehärtet (kein `SELECT INTO`, keine Mehrfach-Statements, erweiterte Denylist). Offen ist nur, ob `mssql.query` im Server-Dry-Run weiter live liest. | Live-Lesevorschau behalten (der Validator ist dann dicht), dazu in der Doku empfehlen, für MSSQL nur einen `db_datareader`-Benutzer zu verwenden. | Im Dry-Run simulieren (keine echte Vorschau mehr). |
| G12 | C-A30 (niedrig, Desktop) | `SendCompose` und `UpdateComposeDraft` nehmen beliebige Host-Pfade als Anhang an. Ein kompromittierter Renderer könnte so beliebige Dateien per Mail verschicken. Das setzt eine Renderer-Kompromittierung voraus (Sandbox ist aktiv, kein XSS bekannt). | Grant-Register im Main-Prozess: Picker und Drag-and-drop (über Preload) geben Pfade frei, App-eigene Verzeichnisse sind erlaubt, gespeicherte Entwürfe behalten ihre Pfade. | Als Restrisiko dokumentieren. |
